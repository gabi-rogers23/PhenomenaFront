import React from "react";
import { useHistory } from "react-router-dom";

const Home = (props) => {
    return (
        <div>
            <div className="container">
                <h1>PHENOMENA</h1>
                <button>ENTER</button>
            </div>
        </div>
    )
};

export default Home;
